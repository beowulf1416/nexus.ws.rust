[package]
name = "token"
version = "0.1.0"
edition = "2024"

[dependencies]
tracing = "*"

hmac = "*"
sha2 = "*"
jwt = "*"

uuid = { version = "*", features = ["v4"] }

serde = { version = "*", features = ["derive"] }
serde_json = "*"
chrono = { version = "0.4.19", features = [ "serde" ] }
