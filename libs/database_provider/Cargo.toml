[package]
name = "database_provider"
version = "0.1.0"
edition = "2024"

[dependencies]
tracing = "*"

sqlx  = { version="*", features=["runtime-tokio", "postgres"] }



# projects
config = { path = "../config" }