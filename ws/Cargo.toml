[package]
name = "ws"
version = "0.1.0"
edition = "2024"

[dependencies]
tracing = "*"
tracing-subscriber = "*"

# env_logger = "*"

serde = { version="*", features=["derive"]}
serde_json = "*"
envy = "*"

openssl = "*"

actix-web = { version="4", features=["openssl"] }
actix-http = "*"

futures = "*"


# projects
db_provider = { path = "../libs/db_provider" }